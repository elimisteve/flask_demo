[project]
name = "flask-demo"
version = "0.1.0"
description = "Simple Flask app to show people who are new to programming"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
